<nav class="navbar navbar-expand-lg navbar-light bg-light super-navbar">
    <div class="container-fluid">
        <a class="navbar-brand text-sm" href="/">
            Магазин курсов
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link {{#if isHome}} active {{/if}}" aria-current="page" href="/">Главная</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link {{#if isCourses}} active {{/if}}" href="/courses">Курсы</a>
                </li>
                {{#if isAuth}}
                    <li class="nav-item">
                        <a class="nav-link {{#if isCart}} active {{/if}}" href="/cart">Корзина</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{#if isOrders}} active {{/if}}" href="/orders">Заказы</a>
                    </li>
                {{/if}}
            </ul>
            {{#if isAuth}}
                {{#unless isAdd}}
                    <form class="container-fluid justify-content-start">
                        <a href="/add" class="btn btn-primary me-2" type="button">Добавить курс</a>
                    </form>
                {{/unless}}
            {{/if}}

            {{#unless isAuth}}
                <div class="container-fluid"></div>
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link" href="/auth/login">Войти</a>
                    </li>
                </ul>
            {{/unless}}

            {{#if isAuth}}
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link" href="/auth/logout">Выйти</a>
                    </li>
                </ul>
            {{/if}}
        </div>
    </div>
</nav>

<br>