<!doctype html>
<html lang="en">
{{> head }}
<style>
    html,
    body {
        height: 100%;
    }

    body {
        display: -ms-flexbox;
        display: -webkit-box;
        display: flex;
        -ms-flex-align: center;
        -ms-flex-pack: center;
        -webkit-box-align: center;
        align-items: center;
        -webkit-box-pack: center;
        justify-content: center;
        padding-top: 40px;
        padding-bottom: 40px;
        background-color: white;
    }

    .form-sign {
        width: 100%;
        max-width: 330px;
        padding: 15px;
        margin: 0 auto;
    }

    .form-sign .checkbox {
        font-weight: 400;
    }

    .form-sign .form-control {
        position: relative;
        box-sizing: border-box;
        height: auto;
        padding: 10px;
        font-size: 16px;
    }

    .form-sign .form-control:focus {
        z-index: 2;
    }

    .form-sign input {
        background-color: #fafafa;
    }

    .form-sign input:focus {
        background-color: #fafafa;
        outline: none;
        border: 1px solid #4221FD;
        box-shadow: none;
    }

    .form-sign input.sign-top {
        margin-bottom: -1px;
        border-bottom-right-radius: 0;
        border-bottom-left-radius: 0;
    }

    .form-sign input.sign-middle {
        margin-bottom: -1px;
        border-radius: 0;
    }

    .form-sign input.sign-bottom {
        margin-bottom: 10px;
        border-top-left-radius: 0;
        border-top-right-radius: 0;
    }
</style>
<body class="text-center">
<a href="/" style="position: absolute; top: 1rem; left: 1rem">Вернуться в магазин</a>
{{{ body }}}
{{> footer }}
</body>